```bash
# 列出所有的 fpga
lspci | grep Xi
==
1a:00.0 Processing accelerators: Xilinx Corporation Device 500c # c/d 代表未成功
1a:00.1 Processing accelerators: Xilinx Corporation Device 500d
==

# 查看机器的详细信息，"v" 的数量表示信息的详细程度
lspci -vv -s 1a:

# 烧录完需要重启来让主机识别 fpga
sudo reboot
```